using System.Collections.Generic;
using UnityEngine;

public class PersonTester : MonoBehaviour {

    // Use this as entry point
    void Start()
    {
        /*
        //start attributuion
        // Source - https://stackoverflow.com/a/52028582
        // Posted by Felipe Augusto, modified by community. See post 'Timeline' for change history
        // Retrieved 2026-03-05, License - CC BY-SA 4.0
        List<Person> People = new List<Person>();
        //end attribution

        //initiaise a counter
        int counter = 0;
        //uses the while loopn to create 5 person objects
        while (counter < 5)
        {
            People.Add(new Person(60000, 7f));
            Debug.Log("a person was created");
            //this is just here so that i can tell how many people are made more easily
            counter += 1;
        }


        //initialise the amount
        int testSalary = 10000;
        float testOpinion = 0.5f;

        foreach (Person p in People) //iterates over the collection
        {
            //cahnges values by a varying amount each iteration
            Debug.Log("changing values");

            p.ChangeSalary(testSalary);
            Debug.Log("new salary is " + p.salary);

            p.ChangeOpinion(testOpinion);
            Debug.Log("new opinion is " + p.opinion);

            //changing the amount of change to apply next iteration
            //this allows me to observe behaviour of each object
            //letting me see if they are independant
            testSalary = testSalary + 5000;
            testOpinion = testOpinion - 0.7f;

        }

    */
    }

    
	
}
